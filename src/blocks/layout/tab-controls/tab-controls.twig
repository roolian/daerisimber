{#
/**
 * Block Name: Tab controls
 */
#}

{% set allowed_blocks = ['daeris/tab-content'] %}


<div id="{{ id }}" class="{{ class }}" >

    {% include [variant_path ~ fields.variant, variant_path ~ 'default.twig'] ignore missing %}

</div>
